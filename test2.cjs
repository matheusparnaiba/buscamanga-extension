const cheerio = require('cheerio'); fetch('https://mangaonline.blue', {headers: {'User-Agent': 'Mozilla/5.0'}}).then(r => r.text()).then(t => { const $ = cheerio.load(t); console.log('IMG:', $('.es-upd-card').first().find('.es-upd-cover img').attr('src')); })
